#include "Pods-UIView+DragDrop.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/UIView+DragDrop" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/UIView+DragDrop"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}